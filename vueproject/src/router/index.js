//官方文件
import Vue from 'vue';
import VueRouter from 'vue-router';

//自訂要有的分頁
import Home from '@/components/HelloWorld';

// 要使用 VueRouter 就要打這一段
Vue.use(VueRouter);

export default new VueRouter({
    
});